conda env list
conda create -n logocls  python=3.7 -y
conda activate logocls
pip3 install torch==1.10.2+cu113 torchvision==0.11.3+cu113 -f https://download.pytorch.org/whl/cu113/torch_stable.html 
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

#onnx-->param,bin
onnx2ncnn demo.onnx demo.param demo.bin
#ncnn model 优化 注意,最后0指的的是fp32，1指的是fp16
ncnnoptimize demo.param demot.bin demo_sim_fp16.param demo_sim_fp16.bin 1

# 解压文件到指定目录
unzip -n datasets.zip -d ./datasets/
# 压缩文件夹当中的所有文件
zip -r myZip.zip ./logs/
#在vscode当中查看训练图标tensorboard
tensorboard --logdir logs/loss_2022_05_02_10_36_11

nvidia-smi #GPU列表信息
nvcc -V 
